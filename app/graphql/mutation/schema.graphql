type Mutation {
  # 手机验证码登录
  # Mobile verification code login
  mobileCodeLogin(
    mobile: String!
    code: String!
    ctry_code: String!
  ): LoginInfo!

  # 修改密码
  resetPassword(
    ctry_code: String!
    mobile: String!
    code: String!
    password: String!
  ): Res!
}
