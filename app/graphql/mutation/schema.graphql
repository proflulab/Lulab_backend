type Mutation {
  # 手机验证码登录
  # Mobile verification code login
  mobileCodeLogin(
    ctry_code: String!
    mobile: String!
    code: String!
  ): LoginInfo!

  # 邮箱验证码登录
  # Email verification code login
  emailCodeLogin(ctry_code: String!, email: String!, code: String!): LoginInfo!
}
